name: Patch Hungarian article

on:
  push:
    branches: [exercise-url-presets]
    paths:
      - '.github/workflows/patch-hungarian-article.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: exercise-url-presets
      - run: python scripts/patch_hungarian_article.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add verbs.py
          git commit -m "Use Hungarian article before verb labels"
          git push origin exercise-url-presets
