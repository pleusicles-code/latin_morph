name: Patch verb exercise modes

on:
  push:
    branches: [exercise-url-presets]
    paths:
      - '.github/workflows/patch-verb-exercise-modes.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: exercise-url-presets
      - run: python scripts/patch_verb_exercise_modes.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add verbs.py
          git commit -m "Add verb inflection and recognition modes"
          git push origin exercise-url-presets
