name: Keep BevLat awake

on:
  schedule:
    - cron: "17 */8 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping BevLat
        run: curl --fail --silent --show-error https://bevlat.streamlit.app/_stcore/health > /dev/null
