name: Patch recognition dictionary style

on:
  push:
    branches: [exercise-url-presets]
    paths:
      - '.github/workflows/patch-recognition-dictionary-style.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: exercise-url-presets
      - run: python scripts/patch_recognition_dictionary_style.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add verbs.py
          git commit -m "Style recognition dictionary entry"
          git push origin exercise-url-presets
