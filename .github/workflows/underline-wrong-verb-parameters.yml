name: Underline wrong verb recognition parameters

on:
  push:
    branches: [exercise-url-presets]
    paths:
      - '.github/workflows/underline-wrong-verb-parameters.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: exercise-url-presets
      - run: python scripts/underline_wrong_verb_parameters.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add verbs.py
          git commit -m "Underline incorrect verb recognition parameters"
          git push origin exercise-url-presets
