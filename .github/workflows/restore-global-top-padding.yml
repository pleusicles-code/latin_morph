name: Restore global top padding

on:
  push:
    branches: [exercise-url-presets]
    paths:
      - '.github/workflows/restore-global-top-padding.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: exercise-url-presets
      - run: python scripts/restore_global_top_padding.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add streamlit_app.py
          git commit -m "Restore global top padding to 2rem"
          git push origin exercise-url-presets
