name: Temporary validate noun number switch

on:
  push:
    branches:
      - exercise-url-presets

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile nouns page
        run: python -m py_compile nouns.py
